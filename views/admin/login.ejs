<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= typeof metaTitle !=='undefined' ? metaTitle : "Admin — L'InviThé Gourmand" %>
    </title>
    <meta name="robots" content="noindex, nofollow">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;700&family=Playfair+Display:wght@600&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body class="bg-creme min-h-screen flex items-center justify-center px-4 font-body">
    <div class="w-full max-w-md">
        <!-- Logo -->
        <div class="text-center mb-8">
            <a href="/" class="inline-flex items-center gap-2">
                <span class="text-3xl">🍵</span>
                <span class="font-display text-2xl text-noir-cafe">L'InviThé Gourmand</span>
            </a>
            <p class="text-sm text-brun/50 mt-1">Espace Administration</p>
        </div>

        <!-- Form -->
        <form method="POST" action="/admin/login" class="bg-blanc-casse rounded-gourmand-lg p-8 shadow-card">
            <h1 class="font-display text-2xl text-noir-cafe mb-6">Connexion</h1>

            <% if (error) { %>
                <div
                    class="bg-rose-macaron/10 border border-rose-macaron/30 text-rose-macaron-dark rounded-gourmand p-3 mb-6 text-sm flex items-center gap-2">
                    <svg class="w-5 h-5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                            d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                    </svg>
                    <%= error %>
                </div>
                <% } %>

                    <div class="space-y-4">
                        <div>
                            <label for="email" class="block text-sm font-medium text-brun mb-1">Email</label>
                            <input type="email" id="email" name="email" required value="<%= email || '' %>"
                                class="w-full px-4 py-3 rounded-gourmand border border-creme focus:border-terracotta focus:ring-2 focus:ring-terracotta/20 outline-none transition-all"
                                autocomplete="email">
                        </div>
                        <div>
                            <label for="password" class="block text-sm font-medium text-brun mb-1">Mot de passe</label>
                            <input type="password" id="password" name="password" required
                                class="w-full px-4 py-3 rounded-gourmand border border-creme focus:border-terracotta focus:ring-2 focus:ring-terracotta/20 outline-none transition-all"
                                autocomplete="current-password">
                        </div>
                    </div>

                    <button type="submit"
                        class="w-full mt-6 py-3 bg-terracotta text-blanc-casse font-medium rounded-gourmand hover:bg-terracotta-dark transition-all shadow-warm">Se
                        connecter</button>
        </form>

        <p class="text-center text-xs text-brun/30 mt-6"><a href="/" class="hover:text-terracotta transition-colors">←
                Retour au site</a></p>
    </div>
</body>

</html>