<% layout('layouts/main') -%>

    <section class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 py-20 text-center">
        <!-- Force clear frontend cart -->
        <script>
            document.addEventListener('alpine:init', () => {
                const cart = Alpine.store('cart');
                if (cart) {
                    cart.items = [];
                    cart.count = 0;
                    cart.total = 0;
                    cart.subtotal = 0;
                }
            });
        </script>
        <% if (order) { %>
            <!-- Confettis animation -->
            <div class="relative inline-block mb-8" data-aos="zoom-in">
                <div class="w-24 h-24 bg-vert-the/10 rounded-full flex items-center justify-center mx-auto">
                    <svg class="w-12 h-12 text-vert-the" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                    </svg>
                </div>
            </div>

            <h1 class="font-display text-3xl sm:text-4xl text-noir-cafe" data-aos="fade-up">Merci pour votre commande !
            </h1>
            <p class="font-accent italic text-lg text-brun/60 mt-3" data-aos="fade-up" data-aos-delay="100">Votre
                commande <strong class="text-terracotta">
                    <%= order.orderNumber %>
                </strong> est confirmée.</p>
            <p class="text-sm text-brun/50 mt-2" data-aos="fade-up" data-aos-delay="200">Un email de confirmation a été
                envoyé à <strong>
                    <%= order.customerEmail %>
                </strong></p>

            <!-- Détails -->
            <div class="bg-blanc-casse rounded-gourmand-lg p-6 shadow-card mt-10 text-left" data-aos="fade-up"
                data-aos-delay="300">
                <h2 class="font-display text-lg text-noir-cafe mb-4">Détails de votre commande</h2>

                <div class="space-y-3">
                    <% order.items.forEach(function(item) { %>
                        <div class="flex justify-between text-sm">
                            <span class="text-brun">
                                <%= item.productName %> × <%= item.quantity %>
                            </span>
                            <span class="text-brun font-medium">
                                <%= formatPrice(item.totalPrice) %>
                            </span>
                        </div>
                        <% }); %>
                </div>

                <div class="border-t border-creme mt-4 pt-4 space-y-2">
                    <div class="flex justify-between text-sm"><span>Sous-total</span><span>
                            <%= formatPrice(order.subtotal) %>
                        </span></div>
                    <% if (Number(order.discount)> 0) { %>
                        <div class="flex justify-between text-sm text-vert-the"><span>Réduction</span><span>-<%=
                                    formatPrice(order.discount) %></span></div>
                        <% } %>
                            <div class="flex justify-between text-sm"><span>Frais de port</span><span>
                                    <%= Number(order.shippingCost)> 0 ? formatPrice(order.shippingCost) : 'Gratuit' %>
                                </span></div>
                            <div
                                class="flex justify-between font-display text-lg text-noir-cafe pt-2 border-t border-creme">
                                <span>Total payé</span><span class="text-terracotta">
                                    <%= formatPrice(order.total) %>
                                </span>
                            </div>
                </div>

                <!-- Livraison -->
                <div class="bg-creme rounded-gourmand p-4 mt-4">
                    <% if (order.deliveryMethod==='pickup' ) { %>
                        <p class="text-sm text-brun">📍 <strong>Retrait en boutique</strong> — 64, rue d'Alésia, 75014
                            Paris</p>
                        <% if (order.deliverySlot) { %>
                            <p class="text-sm text-brand-brown font-medium mt-1">📅 Créneau : <%= order.deliverySlot %>
                            </p>
                            <% } %>
                                <p class="text-xs text-brun/50 mt-1">Votre commande sera prête sous 24h. Nous vous
                                    enverrons un
                                    email.</p>
                                <% } else { %>
                                    <p class="text-sm text-brun">🚚 <strong>Livraison à domicile</strong></p>
                                    <% if (order.deliverySlot) { %>
                                        <p class="text-sm text-brand-brown font-medium mt-1">📅 Créneau : <%=
                                                order.deliverySlot %>
                                        </p>
                                        <% } %>
                                            <p class="text-xs text-brun/50 mt-1">Expédition sous 48h. Un email de suivi
                                                vous sera
                                                envoyé.</p>
                                            <% } %>
                </div>
            </div>

            <div class="flex flex-col sm:flex-row gap-4 justify-center mt-10" data-aos="fade-up" data-aos-delay="400">
                <a href="/commande/<%= order.id %>/facture"
                    class="px-8 py-3 bg-white border-2 border-terracotta text-terracotta rounded-gourmand font-medium hover:bg-terracotta hover:text-white transition-colors shadow-sm flex items-center justify-center gap-2">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                            d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                    </svg>
                    Télécharger la facture
                </a>
                <a href="/boutique"
                    class="px-8 py-3 bg-terracotta text-blanc-casse rounded-gourmand font-medium hover:bg-terracotta-dark transition-colors shadow-warm">Continuer
                    mes achats</a>
                <a href="/"
                    class="px-8 py-3 border border-creme text-brun rounded-gourmand hover:border-terracotta hover:text-terracotta transition-colors">Retour
                    à l'accueil</a>
            </div>
            <% } else { %>
                <div class="py-20">
                    <svg class="w-20 h-20 mx-auto text-brun/20 mb-4" fill="none" stroke="currentColor"
                        viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1"
                            d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                    </svg>
                    <h1 class="font-display text-2xl text-brun/40">Commande introuvable</h1>
                    <a href="/"
                        class="inline-block mt-6 px-6 py-3 bg-terracotta text-blanc-casse rounded-gourmand hover:bg-terracotta-dark transition-colors">Retour
                        à l'accueil</a>
                </div>
                <% } %>
    </section>